import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    padding: 20,
  },
  heading: {
    textAlign: 'center',
    fontWeight: 'bold',
    fontSize: 20,
    marginBottom: 16,
  },
  product: {
    borderWidth: 1,
    borderColor: 'black',
    marginBottom: 16,
  },
  input: {
    borderWidth: 1,
    borderColor: 'teal',
    borderRadius: 10,
    marginTop: 16,
    marginBottom: 16,
  },
});

export default styles;
